# Project workflow

At the end of each working session, if project changes have been committed locally, push the current branch to the configured GitHub remote and confirm that the working tree is clean. Do not push uncommitted changes; report them instead.
